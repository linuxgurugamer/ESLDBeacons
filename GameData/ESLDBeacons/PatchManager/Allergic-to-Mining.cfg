PatchManager
{
	// Required settings.  
	// srcPath should use forward slashes, and include the full file name.  srcPath should be in a directory 
	// called ".../PatchManager/PluginData"
	modName = ESLDBeacons
	patchName = Allergic to Mining
	srcPath = ESLDBeacons/PatchManager/4-Allergic-to-Mining.cfg
	shortDescr = Karborundum in the editor

	// Optional, but recommended
	longDescr = Makes Karborundum available in the editor.  Note that the default setting for Karborundum is tweakable, use this in case it has been changed and you want it in the editor.  It won't hurt if you do enable it


	//// Optional entries here

	// dependencies, only show this patch if these specified mods are available
	// List either the directory of the mod (as show by ModuleManager), or the 
	// mod DLL (as show by ModuleManager)
	//dependencies = 

	// Path to icon, if desired.  Can be a flag
	icon = ESLDBeacons/Flags/ekstra160

	// Author's name, if desired
	author = Booots, Linuxgurugamer 
}

